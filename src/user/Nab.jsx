// import User from "../user/User"

export default function Nab(sata) {
    // const maxbit = typeof(data.bits)== "number"
    //                 ?
    //                 Math.max(data.bids)
    //                 :
    //                 Math.max(data.bids[0])

    // let sortedData = maxbit.sort((a, b) => b- a);
  return (
    <>
        {sata.index.Customer.firstname}
        
    </>
  )
}
